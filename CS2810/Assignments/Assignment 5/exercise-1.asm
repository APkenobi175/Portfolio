# First, make sure the following setting is DISABLED:
# Settings>Permit Extended (pseudo instructions and formats)
# Manually load the following values:
# a = 5, b = 3, c = -6
# Perform the following calculation:
# d = (a - b) + c + 2
# Store d in Register #11
